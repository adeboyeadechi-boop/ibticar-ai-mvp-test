{
  "buildCommand": "npx prisma generate && npx prisma db push --force-reset --accept-data-loss --skip-generate && npx tsx prisma/seed-prod.ts && next build",
  "installCommand": "npm install",
  "framework": "nextjs",
  "env": {
    "PRISMA_GENERATE_SKIP_AUTOINSTALL": "true"
  },
  "crons": [
    {
      "path": "/api/alerts/check",
      "schedule": "0 0 * * *"
    }
  ]
}
