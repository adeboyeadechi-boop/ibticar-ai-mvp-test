{
  "buildCommand": "npx prisma generate && npx prisma db push --accept-data-loss --skip-generate && next build",
  "installCommand": "npm install",
  "framework": "nextjs",
  "env": {
    "PRISMA_GENERATE_SKIP_AUTOINSTALL": "true"
  }
}
