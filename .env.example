# Fichier .env.example
# Copiez ce fichier en .env et remplissez les valeurs appropriées

# =============================================================================
# ENVIRONNEMENT
# =============================================================================
# NODE_ENV est automatiquement géré par Vercel (production/development)
NODE_ENV=development

# =============================================================================
# BASE DE DONNÉES (PostgreSQL)
# =============================================================================
# Variables pour Docker (docker-compose.yml) - Local uniquement
DATABASE_USER=admin_ibticar
DATABASE_PASSWORD=your-secure-password-here
DATABASE_NAME=ibticar_ai_db

# URL de connexion pour Prisma
# LOCAL: Utiliser localhost
DATABASE_URL="postgresql://admin_ibticar:your-secure-password-here@localhost:5432/ibticar_ai_db?schema=public"

# VERCEL: Remplacer par l'URL de votre base de données (ex: Vercel Postgres, Supabase, etc.)
# DATABASE_URL="postgresql://user:password@host:5432/database?schema=public&sslmode=require"

# =============================================================================
# REDIS
# =============================================================================
# Variables pour Redis - Local
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_URL="redis://localhost:6379"

# VERCEL: Utiliser un service Redis externe (ex: Upstash, Redis Cloud)
# REDIS_URL="redis://default:password@host:port"

# =============================================================================
# AUTHENTIFICATION (NextAuth.js)
# =============================================================================
# L'URL de base de ce projet backend
# LOCAL:
NEXTAUTH_URL=http://localhost:3000

# VERCEL: Sera automatiquement défini par Vercel, mais vous pouvez le surcharger
# NEXTAUTH_URL=https://votre-domaine.vercel.app

# Secret pour signer les JWT (généré avec: openssl rand -base64 32)
# IMPORTANT: Générez votre propre secret en production !
NEXTAUTH_SECRET=your-nextauth-secret-here

# Alternative: JWT_SECRET (utilisé comme fallback si NEXTAUTH_SECRET n'est pas défini)
# JWT_SECRET=votre-secret-jwt

# =============================================================================
# INTELLIGENCE ARTIFICIELLE (Anthropic Claude)
# =============================================================================
# Fournisseur d'IA (claude, openai, etc.)
AI_PROVIDER=claude

# Clé API Anthropic - OBLIGATOIRE pour utiliser les fonctionnalités IA
# Obtenez votre clé sur: https://console.anthropic.com
ANTHROPIC_API_KEY=your-anthropic-api-key-here

# URL de base pour l'API AI (optionnel, pour utiliser un proxy ou endpoint custom)
# AI_BASE_URL=https://api.anthropic.com

# Modèle d'IA à utiliser
AI_MODEL=claude-3-5-sonnet-20241022

# Température (0.0 à 1.0) - Contrôle la créativité des réponses
AI_TEMPERATURE=0.7

# Nombre maximum de tokens pour les réponses
AI_MAX_TOKENS=4096

# =============================================================================
# VERCEL (Variables automatiques)
# =============================================================================
# Ces variables sont automatiquement fournies par Vercel lors du déploiement :
# - VERCEL_URL : URL du déploiement (ex: mon-projet-hash.vercel.app)
# - VERCEL_ENV : Environnement (production, preview, development)
# - VERCEL_GIT_COMMIT_SHA : Hash du commit Git
# Vous n'avez pas besoin de les définir manuellement.

# =============================================================================
# INSTRUCTIONS
# =============================================================================
# 1. Copiez ce fichier en .env : cp .env.example .env
# 2. Remplacez toutes les valeurs "your-*-here" par vos vraies valeurs
# 3. Pour générer un secret sécurisé : openssl rand -base64 32
# 4. Ne commitez JAMAIS le fichier .env (il est dans .gitignore)
